1️⃣ What is the difference between var, let, and const?
ans:var is scoped to a function and can be redeclared and given a new value.
Let and const are block-scoped.  It is okay to change the value of let, but not const.


2️⃣ What is the spread operator (...)?
ans:The spread operator (...) breaks up the parts of an array or object into separate values.


3️⃣ What is the difference between map(), filter(), and forEach()?
ans:map() changes each element and gives a new array, while filter() gives elements that meet a certain condition.
The forEach() method just like an array like loop(), for of.

4️⃣ What is an arrow function?
ans:Arrow function is a shorter way to write functions.

5️⃣ What are template literals?
ans:Backticks (`) are used to write template literals, which are strings.  
